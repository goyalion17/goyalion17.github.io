import React from "react";

const Header = () => {
  return (
    <div className="Header">
      <a href="#">Home</a>
      <a href="#">About</a>
      <a href="#">Contact</a>
    </div>
  );
};

export default Header;
